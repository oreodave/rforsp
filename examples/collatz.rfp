[
  [$x x]                                  $force
  [$fn $arg [^arg fn]]                    $partial

  [$self $n
    n print
    n 1 eq if
    ['done print]
    [
      n 1 & 0 eq if
      [n 2 / self]
      [3 n * 1 + self]
    ]
  ] rec $collatz

  'collatz print
  100 collatz
]
